# 問題概要

- `N × N` マスの倉庫がある
- 左上を `(0,0)` とし，下方向に `i`，右方向に `j` 進んだマスを `(i,j)` とする
- 搬出口は `(0, N/2)` に存在する
- 倉庫には `0 ～ N^2-1` の番号付き箱が 1 個ずつ置かれている
- 初期状態で `(i,j)` にある箱番号は `a[i][j]`

---

# ベルトコンベアの設置

最初に，ループ状の装置「ベルトコンベア」を最大 `N^2` 個まで設置できる。

## m 番目のベルトコンベア

以下のマス列で表される。

```text
(i[m][0], j[m][0]),
(i[m][1], j[m][1]),
...
(i[m][l[m]-1], j[m][l[m]-1])
```

ここで：

- `l[m]` はベルトコンベア長
- 各マスはループ状につながる

---

# ベルトコンベアの制約

## 1. 長さ制約

```text
l[m] >= 2
```

## 2. 座標範囲

すべての `x` について：

```text
0 <= i[m][x], j[m][x] < N
```

## 3. 同一ベルト内で重複禁止

```text
(i[m][0], j[m][0]) ～ (i[m][l[m]-1], j[m][l[m]-1])
```

はすべて異なる。

## 4. 隣接制約

各連続マスは上下左右に隣接している。

```text
(i[m][x], j[m][x]) ↔ (i[m][x+1], j[m][x+1])
```

また，最後と最初も隣接している。

## 5. マス共有制約

全ベルトコンベアを通して：

```text
各マスが含まれるベルト数 <= 2
```

---

# 操作

最大 `10^5` ターンまで操作可能。

各ターンでは：

```text
(m, d)
```

を指定する。

- `m` : ベルトコンベア番号
- `d ∈ {-1, 1}` : 回転方向

---

# 回転操作

ベルト上の箱と空きマスが一斉に 1 マス循環移動する。

操作前：

```text
(i[m][x], j[m][x])
```

にあった箱は，操作後：

```text
(i[m][(x+d) mod l[m]],
 j[m][(x+d) mod l[m]])
```

へ移動する。

---

# 搬出ルール

操作後：

- 搬出口 `(0, N/2)` に箱が存在する
- その箱番号が，倉庫内に残っている箱の中で最小である

この 2 条件を満たす場合，その箱は自動的に搬出される。

---

# 特例

初期状態で：

```text
a[0][N/2] == 0
```

なら，箱 `0` は最初の操作前に搬出される。

---

# 目的

できるだけ少ない操作回数で，

```text
0, 1, 2, ..., N^2-1
```

の順にすべて搬出する。

---

# スコア

- `T` : 操作回数
- `B` : 搬出できた箱数

## 全搬出成功：`B = N^2` の場合

```text
10^6 + round(10^6 * log2(T / 10^5))
```

## 未完了：`B < N^2` の場合

```text
round(10^6 * B / N^2)
```

---

# 入力形式

```text
N
a[0][0] ... a[0][N-1]
...
a[N-1][0] ... a[N-1][N-1]
```

---

# 入力制約

## N

```text
N = 20 固定
```

## 箱番号

```text
0 <= a[i][j] <= N^2 - 1
```

## 重複なし

```text
0 ～ N^2-1
```

がちょうど 1 回ずつ現れる。

---

# 出力形式

## 1. ベルトコンベア定義

まずベルト数 `M` を出力する。

```text
M
l[0] i[0][0] j[0][0] ... i[0][l[0]-1] j[0][l[0]-1]
...
l[M-1] i[M-1][0] j[M-1][0] ... i[M-1][l[M-1]-1] j[M-1][l[M-1]-1]
```

## 2. 操作列

続いて，操作回数 `T` と操作列を出力する。

```text
T
m[0] d[0]
...
m[T-1] d[T-1]
```

`t` 回目の操作では，`m[t]` 番目のベルトコンベアを方向 `d[t]` へ 1 マス分だけ循環移動させる。

---

# 出力制約

## ベルト数

```text
0 <= M <= N^2
```

## ベルト長

```text
l[m] >= 2
```

## 座標範囲

```text
0 <= i[m][x], j[m][x] < N
```

## ベルト内部

- マス重複禁止
- 隣接必須
- ループ閉路必須

## マス共有

```text
各マスが属するベルト数 <= 2
```

## 操作数

```text
0 <= T <= 10^5
```

## 操作制約

```text
0 <= m[t] < M
d[t] ∈ {-1, 1}
```
